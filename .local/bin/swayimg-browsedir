#!/usr/bin/env bash

set -e

if [ $# -ne 1 ]; then
    echo "usage: $0 <image>"
    exit 1
fi

IMG="$1"

if [ ! -f "$IMG" ]; then
    echo "file not found: $IMG"
    exit 1
fi

DIR="$(dirname "$(realpath "$IMG")")"

swayimg --gallery "$DIR"
